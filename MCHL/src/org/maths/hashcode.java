package src.org.maths;


/**
 * 在这里给出对类 hashcode 的描述。
 * 
 * @作者（你的名字）
 * @版本（一个版本号或者一个日期）
 */
public class hashcode {

    public static int hash(String input) {
        int out = input.hashCode();
        return out;
    }
}
